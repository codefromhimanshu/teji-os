# Daily Focus — 2026-06-17

> Engine run: cadence_daily | snapshot: 2026-06-17T11:48Z | Notion read-only.
> One active bet (Jarvis, Shaping). Zero ideas in flight. Zero daily-log entries.

## Top 3 Actions Today

1. **Lock down the Jarvis Shaping doc and move it to Running.** The bet was
   created but the row has no Window, no Hypothesis field, no Success Rule,
   no Final End Date in the Notion properties — only in the page body. Copy
   the values from the page body into the properties so the engine can read
   them. — tied to: Jarvis (the only active bet)
2. **Run the 1-week manual probe before opening the 4-week build window.**
   This is the smallest-validation-step from the idea review: 5 seeds × 7
   days × Claude-prompted draft tweets → spreadsheet of accept / edit /
   reject + word-edit-distance. If the manual loop can't hit ≥14
   usable/week, no automation will fix it. — tied to: Jarvis hypothesis
3. **Disambiguate the Success Rule before freezing scope.** "Edits ≤ 20%"
   currently has two possible meanings (% of pieces edited vs. median
   word-distance per piece). Pick one in writing today, then freeze the
   bar. — tied to: Principle 4 (the bar does not move)

## Avoid Today

- Building any part of the Jarvis pipeline before the manual probe runs
  (Principle 1 — validate before building).
- Adding scope to the Shaping doc — "play around with user_map", "remove
  and add seeds at any point" — those are knobs, not BUILD items
  (Principle 3 — scope frozen once set).
- Picking up a second bet. Jarvis is in Shaping, not waiting (Principle 2).

## Principle Most At Risk

**P1 (Validate before building).** The Shaping doc's component list is
build-heavy: seed extraction, dedup+storage, user_map, twitter_algorithm,
combiner, dashboard, tracking. The Constitution says backstage defaults to
manual; automate only when by-hand costs more than building. Today's risk
is opening the build window before proving the manual loop works.

## Waiting On Human

**Count: 4**

**Top item:** the four required decisions from the Jarvis idea review:
1. Accept the 1-week manual probe as precondition to the build window?
2. Rewrite "Edits ≤ 20%" as a single unambiguous formula?
3. Define "seed" and "usable piece" in one sentence each?
4. State the current baseline (tweets/week without Jarvis)?

Until these are answered, the Shaping doc cannot be frozen.

## Experiments Window Watch

- **Jarvis** — no Window set in Notion properties. Page body says
  "Build window: 4 weeks, Run window: 2 weeks" but no start date is fixed.
  **`bet_running_no_window` fires.** Set the Window property before the
  bet leaves Shaping. (Principle 4, high)

---

## Checks fired

- **`bet_running_no_window`** — Jarvis is in Shaping with no Window set;
  one of the values has to be filled before Status → Running. (Principle 4,
  high)
  - Row: Jarvis ([open](https://app.notion.com/p/37eb9df35f95800fb9cad56368236cc3))
  - Action: Open Jarvis in Notion → set the Window date property.
- **`principle_most_at_risk_today`** — P1 (validate before building), as
  detailed above.

## Clean checks

- `task_done_no_consumed_sp` — clean (no tasks in Done state without SP).
- `task_started_no_started_date` — clean (no tasks Started without date).
- `bet_no_status` — clean (all bets have a Status).
- `task_no_status` — clean.
- `task_blocked_no_reason` — clean.
- `task_no_committed_sp` — clean.
- `experiment_window_closing_2d` — clean (no window even set, so nothing
  to close).

## Unresolved checks

None today. The two `notion_view` checks queried for this cadence
(`bet_running_no_window`, hygiene checks) were evaluated against the Bets
DB schema directly since the view block IDs in `os/config/checks.yaml`
are still `TODO`. Fill them in on first weekly run.

---

*Generated by `os/engine/cadence_daily.md` against the Notion snapshot at
2026-06-17T11:48Z. Read-only run. No Notion writes.*
